## Каспийский страж — монорепа (бот + backend)

В этом репозитории теперь два сервиса:
- `bot/` — телеграм‑бот на aiogram.
- `backend/` — Django + DRF backend.

### Структура

- `bot/` — код бота, свой `requirements.txt`, свой `Makefile`.
- `backend/` — код Django‑проекта, свой `requirements.txt`, свой `Makefile`.
- `.gitignore` — общие правила для Python, виртуальных окружений и IDE.

### Бот: установка и запуск (Windows)

Из корня репозитория:

```bash
cd bot
make install   # создаст .venv и поставит зависимости
make run       # запустит бота
```

Перед запуском создайте `bot/.env` на основе `bot/env.example`.

### Backend: установка и запуск (Windows)

Из корня репозитория:

```bash
cd backend
make install   # создаст .venv и поставит минимальные зависимости
make migrate   # применит миграции
make run       # запустит Django‑сервер на 0.0.0.0:8000
```

При необходимости создайте суперпользователя:

```bash
cd backend
make createsuperuser
```

### Совместный запуск бота и backend

- Убедитесь, что backend запущен на `http://localhost:8000`:

```bash
cd backend
make run
```

- Убедитесь, что в `bot/.env` переменная `API_BASE_URL` либо не задана, либо равна `http://localhost:8000` (по умолчанию берётся из `bot/config.py`).
- Запустите бота в отдельном терминале:

```bash
cd bot
make run
```

Теперь бот будет обращаться к API backend по адресу `http://localhost:8000/api/...`.

### Минимальное ручное тестирование связки бот ↔ backend

1. Запустите backend (`cd backend && make run`) и убедитесь, что Swagger доступен по `/api/schema/swagger-ui/`.
2. Через админку или API создайте несколько меток (`/api/markers/`), чтобы было что видеть в списке объявлений.
3. Запустите бота (`cd bot && make run`), откройте диалог и используйте меню:
   - «📤 Отправить проблему» — пройдите весь сценарий и убедитесь, что в Swagger/админке появилась новая метка.
   - «📋 Список объявлений» — проверьте, что метки с backend отображаются в боте, работает пагинация и кнопка «Взять в работу».
   - «👤 Мой профиль» — убедитесь, что отображаются данные из Telegram, без обращения к защищённому профилю backend.

